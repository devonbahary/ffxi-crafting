# ffxi-crafting-react-app

Follow the [README](../api/README.md) for the backend.

## Development
Make sure the port in `package.json` `proxy` points to the port in the backend API's `.env`.

`npm start`